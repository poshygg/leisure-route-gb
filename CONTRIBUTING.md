# 공동작업 가이드

## 작업이 나뉘는 방식

**특징(feature) 단위로 병렬 작업**합니다. 특징 15개가 서로 독립이라 충돌이 거의 없습니다.

특징 정의는 코드가 아니라 [`config/features.csv`](config/features.csv)에 있습니다.
**축을 추가하는 것 = 행을 추가하는 것**입니다. 코드에 하드코딩하지 마십시오.

## 한 사람이 맡는 단위

특징 하나를 맡으면 이걸 하시면 됩니다:

1. `config/features.csv`에서 자기 행 확인 (`source`, `method`, `buffer_m`)
2. `src/leisure_route/io/` 에 데이터 취득 함수 추가
3. 계산은 **`scoring/methods.py`의 4개 함수 중 하나를 재사용** — 새로 쓰지 마십시오
4. `tests/test_features.py` 에 분포 확인 테스트 (min/max/평균/표준편차)
5. PR

계산 방식이 4개(`area_ratio`, `count_weighted`, `proximity`, `presence`)뿐이라,
새 함수를 쓰고 있으면 설계에서 벗어난 것입니다. 축 C~F는 새 함수가 필요해서 v1에서 뺐습니다.

## v1 작업 보드

| 특징 | 담당 | 상태 | 선행 조건 |
|------|------|------|-----------|
| A1 활엽수 | | 대기 | 임상도 신청·승인 |
| A2 침엽수 | | 대기 | 임상도 (A1과 동시) |
| A3 하천 | | 대기 | — |
| A4 해안 | | 대기 | 표준편차 확인 후 채택 판단 |
| A5 초지 | | 대기 | 토지피복 세분류 |
| A6 화목 | | 대기 | **수종 표기 정규화 사전 필요** |
| B1~B3 유산 | | **차단** | **국가유산 SHP 중분류 필드 확인** |
| B4 전통마을 | | 대기 | — |
| B5 조형물 | | 대기 | 지오코딩 |
| B6 특화거리 | | 대기 | 지오코딩 |
| Z1·Z2 | | 대기 | 토지피복 (A5와 동시) |
| Z3 간선도로 | | 대기 | — |
| 앵커 수집 | | 대기 | 명승·지질명소·공원·국가숲길 |

**B1~B3은 차단 상태**입니다. 국가유산 SHP 속성테이블에 사찰/석탑/성곽 수준의 중분류가
없으면 3개를 1개로 합쳐야 합니다. 먼저 확인하고 시작하십시오.

## 절대 하지 말 것

### 1. 앵커를 특징에 넣기

명승 · 지질명소 · 도립·군립공원 · 국가숲길은 **검증용 앵커**입니다.
특징으로 쓰면 AUC가 조용히 무의미해집니다. 국가유산 데이터를 받으면
**종목코드로 명승을 먼저 분리**하십시오.

### 2. 손으로 라벨링

구간 라벨("이 500m는 숲길")은 **임계값으로 유도**합니다. 사람이 붙이면:
- 재현 불가
- 임계값 바꿀 때마다 전부 다시 해야 함

속성 라벨("이 폴리곤은 활엽수림")은 이미 공공데이터에 있습니다. 다시 만들지 마십시오.

### 3. 데이터 파일 커밋

`data/` 는 gitignore입니다. 용량 + 각 기관 이용약관 문제입니다.
취득 방법만 `docs/01_data_sources.md` 에 기록하십시오.

### 4. `.env` 커밋

`.env.example` 을 복사해서 쓰십시오. 서비스키는 개인 발급입니다.

## 코드 규칙

```bash
ruff check src tests
pytest
```

- 좌표계: 내부 계산은 **EPSG:5179** (통일), 입출력만 EPSG:4326
- 모든 특징 점수는 **0~1 정규화** 후 부호 적용
- 함수는 GeoDataFrame in / Series out

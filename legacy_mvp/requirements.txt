# --- 필수 ---
osmium>=4.3          # pyosmium. PBF 2패스 파서 (경북 규모의 핵심)
osmnx>=2.0           # graph_to_gdfs / nearest_nodes / 지오코딩 + Overpass 폴백 경로
networkx>=3.0        # Dijkstra, 그래프 자료구조
geopandas>=0.14      # sjoin / sjoin_nearest 공간 집계
shapely>=2.0         # 버퍼·교차·LineString
pyproj>=3.5          # WGS84 -> UTM 투영 (버퍼를 m 단위로 쓰려면 필수)
numpy>=1.24
pandas>=2.0
folium>=0.15         # 결과 지도 HTML

# --- 선택 ---
# rasterio>=1.3      # --dem (경사/gentle 축). 현재 미설치 -> --dem 쓰면 실패한다
# anthropic>=0.116   # --llm (자연어 파싱). 0.116.0 에서 output_config 지원 확인함
